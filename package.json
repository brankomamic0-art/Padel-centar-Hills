{
  "name": "padel-centar-hills",
  "version": "1.0.0",
  "description": "Padel centar Hills – static marketing site",
  "scripts": {
    "start": "serve ui_kits/website -l ${PORT:-3000} --no-clipboard",
    "build": "echo 'Static site – nothing to build.'"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
